Name: test-med
SchemaVersion: "1.0"

Actions:
  Build:
    Identifier: aws/build@v1
    Inputs:
      Sources:
        - WorkflowSource
    Configuration:
      Steps:
        - Run: yum install g++
        - Run: yum install build-essential
        - Run: pip install -r requirements.txt